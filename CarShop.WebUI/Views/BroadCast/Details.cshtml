@model GetByIdBroadcastDTO

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<header class="header">
    <div>
        <h2>Duyuru Detayı</h2>
        <p style="color: rgba(255, 255, 255, 0.7); margin-top: 0.5rem;">Duyuruya ait detaylı bilgilere buradan ulaşabilirsiniz.</p>
    </div>
</header>

<div class="detail-card shadow">
    <h4 class="mb-4">@Model.Title Detayları</h4>
    <div class="row">
        <div class="col-md-6">
            <div class="detail-item">
                <i class="fas fa-id-badge"></i> <strong>Duyuru ID:</strong> <span>@Model.BroadcastId</span>
            </div>
            <div class="detail-item">
                <i class="fas fa-tag"></i> <strong>Başlık:</strong> <span>@Model.Title</span>
            </div>
        </div>
        <div class="col-md-6">
            <div class="detail-item">
                <i class="fas fa-heading"></i> <strong>Küçük Başlık:</strong> <span>@Model.SmallTitle</span>
            </div>
        </div>
    </div>
    <div class="mt-4">
        <div class="detail-item align-items-start">
            <i class="fas fa-check-circle"></i> <strong>Aktif Mi?:</strong> <span>@(Model.IsActive ? "Evet" : "Hayır")</span>
        </div>
    </div>

    <div class="d-flex justify-content-end mt-4">
        <a asp-action="Index" class="btn btn-secondary me-2">
            <i class="fas fa-arrow-left"></i> Duyurulara Geri Dön
        </a>
    </div>
</div>