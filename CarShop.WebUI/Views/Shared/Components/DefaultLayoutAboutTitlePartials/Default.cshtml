@model List<ResultAboutDTO>
@foreach (var item in Model)
{
    <div class="col-lg-12">
        <div class="section-title about-title">
            <h2>@item.Title <br />@item.SmallTitle</h2>
            <p>
                @item.Description
            </p>
        </div>
    </div>
}
